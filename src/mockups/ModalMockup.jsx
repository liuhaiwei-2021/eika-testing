export default function ModalMockup() {
	return (
		<div>
			<h2>ModalMockup</h2>
			<p>
				This mockup only exist to test if the Portal Modal can render any kind of modal
				inside.
			</p>
		</div>
	);
}
